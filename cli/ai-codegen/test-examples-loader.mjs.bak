import { ExamplesLoader } from './dist/index.js';

const loader = new ExamplesLoader();

console.log('🧪 Testing ExamplesLoader...\n');

// Test 1: Load index
console.log('📊 Test 1: Load index');
const index = await loader.loadIndex();
console.log(`   Components: ${Object.keys(index.capabilities).length}`);
console.log(`   Patterns: ${index.patterns.length}`);
console.log(`   ✅ Pass\n`);

// Test 2: Get critical examples
console.log('⭐ Test 2: Get critical examples');
const critical = await loader.getCriticalExamples();
console.log(`   Found: ${critical.length} critical examples`);
critical.slice(0, 5).forEach(ex => {
  console.log(`   - ${ex.id}`);
});
console.log(`   ✅ Pass\n`);

// Test 3: Get examples for rule
console.log('🔧 Test 3: Get examples for rule #13');
const rule13 = await loader.getExamplesForRule('13');
console.log(`   Found: ${rule13.length} examples`);
rule13.forEach(ex => {
  console.log(`   - ${ex.id}`);
});
console.log(`   ✅ Pass\n`);

// Test 4: Search by component
console.log('🔍 Test 4: Search VcSelect examples');
const vcselect = await loader.getComponentExamples('VcSelect');
console.log(`   Found: ${vcselect.length} examples`);
console.log(`   ✅ Pass\n`);

// Test 5: Search by tags
console.log('🏷️  Test 5: Search by tags');
const slotExamples = await loader.search({ tags: ['slot'] });
console.log(`   Found: ${slotExamples.length} slot examples`);
console.log(`   ✅ Pass\n`);

console.log('🎉 All tests passed!');
