{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "demo": "lerna run serve --stream --scope demo-manager",
    "storybook:ui": "lerna run storybook:serve --stream --scope @virtocommerce/ui-kit",
    "build": "lerna run build",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
