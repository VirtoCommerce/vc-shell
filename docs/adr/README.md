# Architecture decision records

Records of decisions that shape the framework and are expensive to revisit. Read these before proposing a structural change: a decision recorded here was made deliberately and should be reopened only with new information.

Files are named `NNNN-short-title.md`, numbered sequentially from `0001`. Each follows the Nygard format: Title, Status, Date, Context, Decision, Consequences.
